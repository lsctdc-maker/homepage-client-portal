# 앱 설정
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# NAS 연동 설정 (선택사항 - 데모에서는 로컬 저장만 사용)
# NAS_HOST=your-nas-ip
# NAS_USERNAME=your-nas-username
# NAS_PASSWORD=your-nas-password
# NAS_BASE_PATH=/projects

# 이메일 설정 (실제 환경에서 설정)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM="Your Company" <your-email@domain.com>

# 관리자 설정
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_SECRET_KEY=demo-admin-key

# 크론잡 설정
CRON_SECRET=demo-cron-secret

# SMS 설정 (선택사항)
# SMS_API_KEY=your-sms-api-key
# SMS_SECRET=your-sms-secret

# 데이터베이스 (실제 환경에서 사용)
# DATABASE_URL=postgresql://user:password@localhost:5432/homepage_portal
# REDIS_URL=redis://localhost:6379